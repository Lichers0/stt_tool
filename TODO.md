# TODO

## Bugs

- [x] ~~Overlay окно записи появляется на основном мониторе вместо того, где находится активное приложение. Исправлено: screenForApp теперь использует CGWindowList вместо AX API.~~ (v1.3.3)
- [ ] Окно Sparkle update появляется посередине/внизу экрана вместо того экрана, где развёрнуто окно приложения. Нужно привязать к активному дисплею.
- [x] ~~Переключение vocabulary на "Default" не работает — всегда используется последний выбранный vocabulary. Исправлено: добавлен resolveVocabularyForSession(), objectWillChange для startupMode/defaultVocabularyId, редизайн футера Vocabulary Manager.~~ (v1.3.3)
- [x] ~~Проверить, сохраняется ли выбранный vocabulary между перезапусками приложения (persist через UserDefaults). Исправлено: корректная синхронизация activeVocabularyId при смене режимов, fallback на default при удалении last used.~~ (v1.3.3)
- [ ] При потере соединения (WebSocket/network) нет никакой индикации пользователю. Нужно показывать статус "нет соединения" в UI и/или иконке menu bar.
- [ ] После потери соединения при нажатии paste вставляется текст из предыдущей транскрипции. Буфер не очищается при обрыве — нужно сбрасывать при disconnect/ошибке.
- [ ] Непонятно что означает прыгающая зелёная точка в menu bar — нет пояснения для пользователя. Нужна документация или tooltip.
- [ ] После cancel (Escape) или завершения записи, при следующем нажатии hotkey точка сразу зелёная вместо жёлтой (connecting). WebSocket видимо не закрывается корректно или состояние не сбрасывается.
- [ ] После проблемы с сетью (VPN disconnect и т.д.) соединение не восстанавливается автоматически — приходится перезапускать приложение. Нужен auto-reconnect при восстановлении сети.
- [ ] Во время произношения текста иногда теряются отдельные слова. Нужно разобраться с причиной — возможно проблема в буферизации аудио, потере WebSocket-пакетов или в логике склейки partial/final результатов транскрипции.

## Research

- [ ] Разобраться с механизмом вставки текста. Сейчас используется Accessibility API (AXValue). SuperWhisper использует Cmd+V (симуляция клавиш). Проблема: некоторые приложения не детектят программную вставку через AX — считают поле пустым и не обрабатывают последующие действия (Enter и т.д.). Исследовать: какой метод надёжнее, нужно ли переключиться на Cmd+V, или комбинировать подходы.

## Improvements

- [ ] Начинать запись аудио сразу при нажатии hotkey, ещё до установки WebSocket соединения. Буферизовать аудио и отправить накопленное после connect, чтобы не терять первые слова.
